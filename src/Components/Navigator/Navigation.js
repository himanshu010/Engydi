import React from "react";

const Navigator = () => {
  return (
    <div className="f3 link dim black pa3 pointer underline">
      <nav style={{ display: "flex", justifyContent: "flex-end" }}>
        Sign Out
      </nav>
    </div>
  );
};

export default Navigator;
